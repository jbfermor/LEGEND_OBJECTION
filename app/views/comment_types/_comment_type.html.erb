<div id="<%= dom_id comment_type %>">
  <div class="card ">
    <div class="card-header m-2 p-3 d-flex justify-content-between">
      <h2><%= comment_type.name %></h2>
      <div class="btn-group">
        <div>
          <%= link_to "Editar", edit_comment_type_path(@comment_type), class: "btn btn-sm btn-warning" %>
        </div>
        <%= button_to "Eliminar", @comment_type, method: :delete, class: "btn btn-sm btn-danger" %>
      </div>
    </div>
  </div>   
</div>
