<div class="row">
  <%= form_with(model: comment_type) do |form| %>
  <% if comment_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comment_type.errors.count, "error") %> prohibited this comment_type from being saved:</h2>

      <ul>
        <% comment_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-2">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="mt-2">
    <%= form.submit "Guardar Tipo de Comentario", class: "btn btn-sm btn-success" %>
  </div>
<% end %>
</div>

